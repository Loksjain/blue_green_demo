apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: bluegreen-demo
spec:
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: web
    version: blue
